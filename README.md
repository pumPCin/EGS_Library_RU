EGS Library RU — это пользовательский скрипт (userscript) для магазина Epic Games Store, который добавляет наглядные визуальные метки (плашки) на карточки игр, показывая статус владения ими. Скрипт разработан для русскоязычной версии магазина.

Скрипт помогает мгновенно определять, какие игры уже есть в вашей библиотеке, а какие — нет, не заходя на страницу каждой игры.

🚀 Основные возможности

Наглядная индикация: Добавляет цветные плашки в левый верхний угол карточек игр, DLC и наборов.

Умное кэширование: Скрипт запоминает статус проверенных игр. При повторном посещении страницы данные берутся из памяти браузера мгновенно, не нагружая сервер.

Поддержка наборов (Bundles): Корректно работает на страницах со списками наборов (/bundles/).

Защита от блокировок (Anti-403): Использует систему "умной очереди". Запросы отправляются с задержкой (3 секунды), чтобы избежать временной блокировки (ошибка 403) со стороны Epic Games.

Панель логов: Удобная панель в углу экрана отображает текущий процесс проверки, статус кэша и возникающие ошибки.

Авто-обновление: При заходе на страницу конкретной игры скрипт автоматически обновляет её статус в кэше.

🎨 Легенда статусов и цветов

Плашки и логи имеют цветовую кодировку для быстрого восприятия:

В БИБЛИОТЕКЕ

Цвет плашки: Зеленый (при проверке) / Синий (из кэша).

Значение: Игра уже куплена или получена бесплатно.

НЕ КУПЛЕНО

Цвет плашки: Красный (при проверке) / Оранжевый (из кэша).

Значение: Игры нет на аккаунте.

НЕДОСТУПНО

Цвет плашки: Темно-серый (при проверке) / Черный (из кэша).

Значение: Игра недоступна для покупки.

НЕТ ИГРЫ

Цвет плашки: Светло-серый (при проверке) / Черный (из кэша).

Значение: Это дополнение (DLC), для которого у вас нет базовой версии игры ("Необходима базовая версия").

⚙️ Управление панелью логов

В правом нижнем углу экрана находится панель управления кэшем.

Кнопки очистки:

[К] (Красная) — Удалить ВСЁ: Полностью очищает кэш скрипта и перезагружает страницу. Используйте, если данные кажутся устаревшими.

[В] (Синяя) — Удалить "В библиотеке": Удаляет из памяти только купленные игры.

[Н] (Оранжевая) — Удалить "Не куплено": Удаляет из памяти игры, которых у вас нет (полезно после покупки новых игр).

[Д] (Бирюзовая) — Удалить дубликаты: Сканирует кэш на наличие конфликтующих записей и исправляет ошибки дублирования.

Управление видом:

[-] / [+] — Свернуть или развернуть панель логов.

🛠 Установка

Установите расширение для браузера Tampermonkey (Chrome, Firefox, Edge, Opera).

Перейдите по ссылке установки скрипта (или создайте новый скрипт и вставьте код вручную).

Зайдите на сайт store.epicgames.com.

⚠️ Важная информация

Скорость работы: Скрипт намеренно делает паузы между запросами к серверу (3 секунды). Это не зависание, это защита вашего доступа к магазину.

Ошибка 403: Если сервер Epic Games временно блокирует частые запросы, скрипт автоматически встанет на паузу на 60 секунд (об этом будет написано в логе), а затем продолжит работу.

Язык: Скрипт ориентируется на русский текст интерфейса ("В библиотеке", "Недоступно", …). Убедитесь, что интерфейс магазина включен на русском языке.
